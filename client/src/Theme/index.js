import {createTheme } from "@material-ui/core/styles";

const theme = createTheme({
    palette: {
      type: 'dark',
      background: {
        default: "#C70039"
      },
      primary: {
        main: "#FF3333",
      },
      secondary: {
        main: "#b552f7",
      },
    },
  });

  export default theme;